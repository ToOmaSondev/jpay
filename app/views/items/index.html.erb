<div class="container">


  <div class="cards-index">
    <% @items.each do |item| %>
      <%= link_to item_path(item) do %>
        <%= render 'shared/card', item: item %>
      <% end %>
    <% end %>
    </div>
</div>
